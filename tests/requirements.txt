# Test dependencies. The published packages are pinned to the versions the
# starter itself uses (see README "Versions").
pytest==9.0.2
aac-cli==0.1.5
aac-invoke-auth[fastapi]==0.1.2
httpx==0.28.1
PyYAML==6.0.3
